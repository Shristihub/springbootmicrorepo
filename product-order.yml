#database specific configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springorderdb
    username: root	
    password: root

#jpa specific configuration
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update			
 
